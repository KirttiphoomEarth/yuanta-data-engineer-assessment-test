services:

  # ── Airflow (standalone = webserver + scheduler in one container) ──────────
  airflow:
    image: apache/airflow:2.9.3
    command: standalone
    user: "0"                                           # run as root — avoids all volume permission issues
    environment:
      AIRFLOW__CORE__EXECUTOR: SequentialExecutor        # compatible with SQLite metadata DB
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
      AIRFLOW__CORE__DAGS_FOLDER: /opt/airflow/dags
      AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true" # prevent auto-run before pipeline_initial runs
      AIRFLOW__CORE__DAGBAG_IMPORT_TIMEOUT: "60"

      # Metadata DB — dedicated directory so it survives container restarts
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: "sqlite:////opt/airflow/meta/airflow.db"

      # Webserver — 1 worker to keep memory usage low
      AIRFLOW__WEBSERVER__WORKERS: "1"
      AIRFLOW__WEBSERVER__WEB_SERVER_MASTER_TIMEOUT: "120"
      AIRFLOW__WEBSERVER__WEB_SERVER_WORKER_TIMEOUT: "120"
      AIRFLOW__WEBSERVER__RATELIMIT_STORAGE_URL: "memory://"  # suppress Flask-Limiter warning

      # Scheduler — reduce CPU / memory footprint
      AIRFLOW__SCHEDULER__PARSING_PROCESSES: "1"
      AIRFLOW__SCHEDULER__MIN_FILE_PROCESS_INTERVAL: "30"
      AIRFLOW__SCHEDULER__DAG_DIR_LIST_INTERVAL: "30"

      # Admin user (created on first boot only)
      _AIRFLOW_WWW_USER_CREATE: "true"
      _AIRFLOW_WWW_USER_USERNAME: admin
      _AIRFLOW_WWW_USER_PASSWORD: admin
    volumes:
      - ./dags:/opt/airflow/dags                        # DAG files
      - ./input:/opt/airflow/input:ro                   # source CSVs (read-only)
      - pipeline-db:/opt/airflow/db                     # target brokerage SQLite DB (shared)
      - airflow-meta:/opt/airflow/meta                  # Airflow metadata DB (persisted separately)
    depends_on:
      - db
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 120s
    restart: on-failure

  # ── SQLite (target database — initialises the db file and stays alive) ─────
  db:
    image: keinos/sqlite3
    user: "0"                                           # run as root so it can write to the Docker volume
    entrypoint: ["/bin/sh", "-c", "chmod 777 /db && sqlite3 /db/brokerage.db 'PRAGMA journal_mode=WAL;' && chmod 666 /db/brokerage.db && tail -f /dev/null"]
    volumes:
      - pipeline-db:/db                                 # shared target DB volume
    restart: on-failure

  # ── SQLite Web Viewer ──────────────────────────────────────────────────────
  sqlite-web:
    image: coleifer/sqlite-web
    entrypoint: sqlite_web
    command: ["-H", "0.0.0.0", "-p", "8081", "/db/brokerage.db"]
    depends_on:
      - db
    volumes:
      - pipeline-db:/db                                 # same volume as airflow target DB
    ports:
      - "8081:8081"
    restart: on-failure

volumes:
  pipeline-db:    # shared target SQLite database (brokerage.db)
  airflow-meta:   # Airflow metadata DB (airflow.db) — persisted across container restarts
